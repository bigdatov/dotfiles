#!/bin/bash

# cp -r .config ${HOME}
cp -r .fonts ${HOME}
cp .bashrc ${HOME}
cp .bash_profile ${HOME}
cp .bash_aliases ${HOME}
cp .gitmessage ${HOME}
cp .gitconfig ${HOME}
cp .git-completion.bash ${HOME}
# cp .vimrc ${HOME}
