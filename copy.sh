#!/bin/bash

cp -r .fonts ${HOME}
cp .bashrc ${HOME}
cp .profile ${HOME}
cp .bash_aliases ${HOME}
cp .gitmessage ${HOME}
cp .gitconfig ${HOME}
cp .git-completion.bash ${HOME}
cp .tmux.conf ${HOME}
# cp .vimrc ${HOME}
